{
  "name": "pomodoro-timer-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix project\"",
    "install:all": "npm install --prefix backend && npm install --prefix project"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
